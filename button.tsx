import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Navigation } from "@/components/navigation"
import { Footer } from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "AI Automation Agency | Save 20+ Hours Per Week",
  description: "We build and manage AI agents that handle customer support, lead generation, and repetitive tasks for local businesses. Book your free consultation today.",
  keywords: "AI automation, AI agents, customer support automation, lead generation, business automation, AI consulting",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navigation />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
